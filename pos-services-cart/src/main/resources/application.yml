server:
  port: 30081
spring:
  application:
    name: "cart-service"
  cloud:
    zookeeper:
      connect-string: "localhost:30001"
  data:
    mongodb:
      host: "localhost"
      port: 30004
      database: "pos"
